# Workmate - Random User Generator

![Language](https://img.shields.io/badge/language-Kotlin-blue.svg)
![Platform](https://img.shields.io/badge/platform-Android-brightgreen.svg)
![UI](https://img.shields.io/badge/UI-Jetpack_Compose-orange)

**Workmate** — это современное Android-приложение, разработанное в рамках тестового задания. Приложение позволяет генерировать случайных пользователей, используя публичный API [randomuser.me](https://randomuser.me/), и отображает их в элегантном и удобном интерфейсе, построенном полностью на Jetpack Compose.

## 🎨 Дизайн и UI/UX

Дизайн приложения вдохновлен примером, предоставленным в техническом задании. Основной упор был сделан на создание чистого, интуитивно понятного и отзывчивого пользовательского интерфейса с использованием компонентов **Material 3**. Цветовая палитра и композиция экранов нацелены на создание приятного визуального опыта.

## ✨ Ключевые возможности

*   ✅ **Конфигурируемая генерация пользователя:** На главном экране можно выбрать пол и национальность для создания нового пользователя через API.
*   ✅ **Список пользователей сессии:** Приложение хранит и отображает список всех сгенерированных за текущую сессию пользователей.
*   ✅ **Детальные профили:** По клику на пользователя открывается экран с его подробной информацией, включая фото, контакты, адрес и личные данные, представленные в удобных вкладках.
*   ✅ **Современный UI:** Весь интерфейс построен на декларативном фреймворке **Jetpack Compose** с использованием **Material 3**, что обеспечивает плавность анимаций и современный вид.
*   ✅ **Управление состоянием:** Четкое разделение состояний UI (загрузка, успех, ошибка) с помощью `StateFlow` для реактивного обновления интерфейса.
*   ✅ **Навигация:** Бесшовная навигация между экранами с помощью **Jetpack Navigation for Compose**.

## 🛠️ Архитектура и технологии

Проект придерживается принципов **Clean Architecture** в упрощенной, прагматичной форме, разделяя ответственность между слоями для улучшения читаемости и поддержки кода.

*   **Архитектура:** **MVVM** (View - ViewModel - Model) + **Repository Pattern**.
    *   **View (UI Layer):** `@Composable` функции, формирующие экраны (`HomeScreen`, `ListScreen`, `DetailScreen`). Полностью отделены от бизнес-логики.
    *   **ViewModel:** `UserViewModel` управляет состоянием UI, обрабатывает действия пользователя и взаимодействует с `UserRepository`.
    *   **Repository:** `UserRepository` инкапсулирует логику доступа к данным, работая с сетевым слоем.
    *   **Model:** Data-классы (`User`, `Name`, `Location`), описывающие сущности данных, полученные от API.

*   **Технологический стек:**
    *   **Язык:** [Kotlin](https://kotlinlang.org/)
    *   **UI:** [Jetpack Compose](https://developer.android.com/jetpack/compose) & [Material 3](https://m3.material.io/)
    *   **Асинхронность:** [Kotlin Coroutines](https://kotlinlang.org/docs/coroutines-overview.html) и [Flow](https://kotlinlang.org/docs/flow.html) (`StateFlow` для управления состоянием).
    *   **Внедрение зависимостей (DI):** [Hilt](https://dagger.dev/hilt/)
    *   **Навигация:** [Jetpack Navigation for Compose](https://developer.android.com/jetpack/compose/navigation).
    *   **Сеть:** [Retrofit 2](https://square.github.io/retrofit/) для выполнения HTTP-запросов и [Gson](https://github.com/google/gson) для парсинга JSON.
    *   **Загрузка изображений:** [Coil](https://coil-kt.github.io/coil/compose/) (`AsyncImage`).
    *   **Жизненный цикл:** `ViewModel` из AndroidX Lifecycle.

## 🌐 API Integration

Приложение использует публичный API **randomuser.me**.
*   **Основной endpoint:** `https://randomuser.me/api/`
*   **Параметры:** Запросы отправляются с параметрами `gender` и `nat` для получения пользователя с заданными характеристиками.

## 📂 Структура проекта

*   **`data`**: Содержит все, что связано с данными.
    *   **`api`**: `ApiService` (интерфейс Retrofit) и `RetrofitClient`.
    *   **`model`**: Data-классы, описывающие структуру ответа API.
*   **`di`**: Hilt-модули для внедрения зависимостей (например, `NetworkModule`).
*   **`domain`**:
    *   **`repository`**: `UserRepository`, ответственный за получение данных из сети.
*   **`presentation`**: UI-слой.
    *   **`navigation`**: `NavGraph.kt` для настройки графа навигации.
    *   **`screens`**: Composable-функции для каждого экрана (`HomeScreen`, `ListScreen`, `DetailScreen`).
    *   **`viewModel`**: `UserViewModel` с UI-логикой и состоянием.
*   **`ui.theme`**: Стандартная папка с темой Jetpack Compose.

## 📈 Возможные улучшения (Roadmap)

*   [x] **Реализовать кеширование:** Интегрировать **Room** для сохранения пользователей в локальной базе данных. Это позволит реализовать полноценный оффлайн-режим и сохранять историю между сессиями, выполнив одно из ключевых требований ТЗ.
*   [ ] **Развить архитектуру:** Выделить **UseCases** (например, `CreateUserUseCase`, `GetAllUsersUseCase`) и определить `UserRepository` как интерфейс в `domain` слое для более строгого следования Clean Architecture.
*   [ ] **Написать тесты:** Добавить Unit-тесты для `ViewModel` и `UserRepository`, а также UI-тесты для Compose-экранов.
*   [ ] **Улучшить DI:** Полностью делегировать создание `Retrofit` Hilt-модулю, избавившись от синглтона `RetrofitClient` для повышения гибкости и тестируемости.
